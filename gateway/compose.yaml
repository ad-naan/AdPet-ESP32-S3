version: '3.8'

services:
  adpet-gateway:
    build: .
    container_name: adpet-gateway
    restart: always
    ports:
      - "8787:8787"
    volumes:
      - ./adpet_gateway.db:/app/adpet_gateway.db
      - ./.env:/app/.env
    environment:
      - TZ=Asia/Shanghai
